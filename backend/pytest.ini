[pytest]
asyncio_mode = auto
testpaths = tests
# Coverage habilitado via Makefile/CI: pytest --cov=services --cov=shared --cov-report=term-missing --cov-fail-under=80
addopts = -v
markers =
    e2e: testes end-to-end que requerem banco de dados real
